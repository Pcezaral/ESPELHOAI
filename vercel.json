{
  "buildCommand": "echo 'BUILD STARTED' && pwd && ls -la && echo 'TRYING CD CLIENT' && cd client && pwd && ls -la && pnpm run build",
  "outputDirectory": "client/dist",
  "installCommand": "echo 'INSTALL STARTED' && pwd && ls -la && echo 'TRYING CD CLIENT' && cd client && pwd && ls -la && pnpm install"
}
